func main()
  var i, j: int
  var c1, c2: char
  var x, y: float
  var Ai: array[10] of int
  var Af: array[10] of float

  swap(x+1, y);

  swap(x, y);
  swap(z, A[0]);
  swap(A[0], A[n-1]);

  swap(3, A[2]);

endfunc