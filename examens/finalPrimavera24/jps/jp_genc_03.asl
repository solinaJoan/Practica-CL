func main()
  var i, j: int
  var c1, c2: char
  var x, y: float
  var Ai: array[10] of int
  var Af: array[10] of float

  // swap(ID1, ID2)  amb ID de tipus basic
  x = 55; y = 99;
  swap(x, y);
  write x; write " "; write y; write "\n";

endfunc