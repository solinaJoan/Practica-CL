func f(a: array[10] of float,
       c : float) : float
  var i : int

  return (a[-23]+a[23]) / (2.0*i);
endfunc

func main()
  var A: array[10] of int
  var k, z: int
  
  z = k*f(A, k) % 2;
  write z + A; write "\n";
  write (v / 4.5) > 'c';
endfunc

  