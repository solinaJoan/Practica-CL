func main()
  var a : array [10] of int
  var i,j : int

  i = 2*a.max + 1;
  while a.max < 3*a[i] do
    a[i] = a[i+1]-5.0;
    if a[i]*7 then
      j = j + a.max;
      i = j>a.max and x;
    endif
  endwhile
endfunc

