func f(i : bool, b:array[10] of int) : int
  var j,a : int
  write (a + b);
  a = a > 1;
  b[j] = b[j!2.1*a[k+1]];
  return a * b[a*2+1!b[3]>0];
endfunc

func main()
  var i, j: int
  var c1, c2: char
  var x, y: float
  var A: array[10] of int
  var B: array[10] of float
  var C: array[10] of char

  j = B[i+1] + f(true, A) + f(x<=0, B);
  x = y * A[j-1!22] + B[i+2!3.3] + A[i!x+1];
  c1 = C[i+j!c2];
  y = x - A[j-1!c1] + A[j+1!'@'] - B[x+2!y-1];
 
  write y+x;
endfunc


